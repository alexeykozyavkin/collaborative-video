name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare static site
        run: |
          set -euxo pipefail
          mkdir -p _site/assets
          cp index.html styles.css overrides.css timeline-v7.css timeline-v8.css timeline-v10.css timeline-v11.css app-v10.js patch-v11.js _site/
          cp assets/willow-label-v1.svg assets/willow-label-v2.svg _site/assets/
          touch _site/.nojekyll
          test -s _site/index.html
          test -s _site/styles.css
          test -s _site/overrides.css
          test -s _site/timeline-v10.css
          test -s _site/timeline-v11.css
          test -s _site/app-v10.js
          test -s _site/patch-v11.js
          grep -q 'timeline-v11.css' _site/index.html
          grep -q 'patch-v11.js' _site/index.html

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload static site
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
