name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare static site
        run: |
          set -euxo pipefail
          mkdir -p _site/assets
          sed 's/app-v8.js?v=8/app-v9.js?v=9/' index.html > _site/index.html
          cp styles.css overrides.css timeline-v7.css timeline-v8.css app-v8.js app-v9.js _site/
          cp assets/willow-label-v1.svg assets/willow-label-v2.svg _site/assets/
          touch _site/.nojekyll
          test -s _site/index.html
          test -s _site/styles.css
          test -s _site/overrides.css
          test -s _site/timeline-v7.css
          test -s _site/timeline-v8.css
          test -s _site/app-v8.js
          test -s _site/app-v9.js
          grep -q 'app-v9.js?v=9' _site/index.html

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload static site
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
