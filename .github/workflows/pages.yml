name: Deploy GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare static site
        run: |
          set -euxo pipefail
          mkdir -p _site/assets
          cp index.html v5.css v7.css v7.js _site/
          cp assets/willow-label-v1.svg assets/willow-label-v2.svg _site/assets/
          touch _site/.nojekyll
          test -s _site/index.html
          test -s _site/v5.css
          test -s _site/v7.css
          test -s _site/v7.js
          grep -q './v7.css' _site/index.html
          grep -q './v7.js' _site/index.html
          grep -q "@import url('./v5.css')" _site/v7.css

      - name: Configure Pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Upload static site
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
